// Not used directly but you can set pycharm to read this file so it knows about the webpack setup
const webpackProjectConfig = require('./webpack.project.config');

module.exports = webpackProjectConfig.development();
