"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = normalize;

var normalizr = _interopRequireWildcard(require("normalizr"));

var _Model = require("./Model");

function normalize(model, data) {
  var schema = model[_Model.$modelDefinition].schema;
  var schemaOptions = model[_Model.$modelDefinition].schemaOptions;

  if (Array.isArray(data)) {
    schema = normalizr.arrayOf(schema);
  }

  return normalizr.normalize(data, schema, schemaOptions);
}