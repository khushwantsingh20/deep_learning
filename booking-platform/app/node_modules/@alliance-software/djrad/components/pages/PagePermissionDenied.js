// @flow
import * as React from 'react';
import { Helmet } from 'react-helmet';

type Props = {
    /* Optionally specify content to render on page */
    children?: React.Node,
    /* Set <title> in <head> and, in default component, used in page <h3>) */
    title?: string,
};

export default function PagePermissionDenied({ children, title = 'Permission Denied' }: Props) {
    if (!children) {
        children = (
            <span>
                <h3>{title}</h3>
                <p>Sorry, you don’t have access to this resource.</p>
            </span>
        );
    }
    return (
        <div>
            <Helmet>
                <title>{title}</title>
            </Helmet>
            {children}
        </div>
    );
}
