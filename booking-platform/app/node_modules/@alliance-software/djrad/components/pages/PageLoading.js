// @flow
import React from 'react';
import { Icon } from 'antd';

import './PageLoading.less';

type Props = {
    message?: string,
};

export default function PageLoading({ message = 'Please wait... loading' }: Props) {
    return (
        <div className="djrad--page-loading">
            <h3>
                <Icon type="loading" /> {message}
            </h3>
        </div>
    );
}
