import { useContext } from 'react';
import DevToolsContext from '../context';

export default function useDevToolsData() {
    const context = useContext(DevToolsContext);
    return context.devToolsData;
}
