// eslint-disable-next-line no-undef
if (process.env.NODE_ENV === 'production') {
    // eslint-disable-next-line no-undef
    module.exports = function devtools() {
        return () => next => action => {
            return next(action);
        };
    };
} else {
    // eslint-disable-next-line no-undef
    module.exports = require('./middleware.dev.js');
}
